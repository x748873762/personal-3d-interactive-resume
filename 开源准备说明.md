# 通用模板公开准备说明

## 本次边界

这是从空目录建立的通用版本。只参考获准的交互说明和最小导航实现，没有整体复制原项目。没有读取或搬运私有目录、个人资料、个人素材、原页面截图、备份、业务工作台源代码或凭据。初始阶段没有远端操作；后续经维护者明确授权，将本通用版本独立公开并部署。

## 已保留与替换

| 结构／交互 | 通用版 |
| --- | --- |
| 双主题 | 红黑、暖灰，全站同步与本地记忆 |
| 角色与岗位便签 | 原创几何角色；五张观察／构想／搭建／打磨／分享便签 |
| 滚动镜头与便签高亮 | 保留，当前叙事控制镜头与便签状态 |
| 经历纸张与翻页 | 五段明确虚构的叙事，每段关联原创图形练习 |
| 横向画廊与预览 | 桌面横向、窄屏纵向；原生弹窗及焦点返回 |
| 能力与联系区域 | 虚构能力示例、回到第一页入口；没有联系方式或下载 |
| 私人模型、照片与业务演示 | 全部不纳入，替换为代码绘制的基础几何图形 |

这是一份保留交互模式的通用重建，不是对原网站的逐像素复制。滚动镜头采用轻量移动与转向；预览只展示原创图形，不提供真实业务表单或在线模型。

## 素材与授权账本

- 本次新写 React/CSS/交互代码：供维护者审阅；项目许可尚未指定。
- 几何角色：Sphere、Capsule、Torus 基础几何体组合，无外部模型、纹理、面部素材。
- 画廊：五幅 CSS 抽象构图，无图库、商标、项目截图或客户内容。
- 字体：仅系统字体栈，不随包分发字体文件。
- 运行时依赖：React、React DOM、Three.js，安装包声明为 MIT；已附运行时许可文本。
- 开发依赖：Vite、React 插件为 MIT；Playwright Test 为 Apache-2.0。完整依赖锁定于 lockfile，开发工具本体不随候选源码目录复制。

## 仍待维护者确认

1. 项目的开源许可证及署名方式。本次未擅自添加 MIT、Apache 或其他项目许可证。
2. 代码与设计对外授权是否符合维护者预期；依赖许可证不自动解决项目本身的授权。
3. 公开目标已确定为 x748873762/personal-3d-interactive-resume；main 仅包含本模板白名单源码，gh-pages 仅包含构建产物。
4. 若日后加入照片、字体、模型或图片，逐项确认再分发许可并更新素材账本。
5. 后续每次发布重新核查依赖安全状态、许可清单、替换后的内容与候选包。

未指定项目许可证；公开展示不自动等于授予开源再分发许可。许可与署名可后续单独确定。

## 匿名化验证范围

候选源码由明确清单生成，额外附 SHA-256 清单。检查人名标记、联系方式、本机绝对路径、私有素材引用与常见凭据签名。检查不打开任何凭据文件。构建无 sourcemap、无外部媒体、无私有文件副本。

本地管理文件、依赖和生成目录不进入候选包。构建中的第三方通用文档链接不等于运行时请求；网络验证另行确认页面不调用外站。

## 本次验证结果

2026-09-13，本地 Playwright + 已安装 Chrome 验证。Browser 技能不可用；Playwright 自带 Chromium 下载未完成，采用已安装 Chrome 通道。

| 检查 | 结果 |
| --- | --- |
| 安装与生产构建 | 通过；白名单候选包在独立临时目录安装、构建、隐私检查通过 |
| 浏览器行为测试 | 6 项通过：主题／导航／五段翻页／画廊／预览／回退 |
| 桌面／笔记本／窄屏 | 1700×1000、1280×720、390×844，已检查截图，无横向溢出 |
| 视觉检查 | 标题换行、角色与便签、纸张翻页、画廊构图、弹窗、页尾与窄屏顺序 |
| 页面与控制台 | 正确标题与内容，无空白页或框架错误遮罩；正常流程无应用错误 |
| 网络边界 | 正常浏览流程请求仅来自本机服务，无外部请求 |
| 匿名化 | 20 项白名单源码文件及生产产物检查通过；虚构邮箱探针会触发拒绝 |
| 原网站参考文件 | 本次读取的四个参考文件前后 SHA-256 相同，无原项目写入操作 |

修复项：画廊末项计数与滚动空间、预览作品的对齐、叙事状态下首屏提示遮挡、角色说明与脚部间距、生产预览站点图标缺失；隐私扫描排除 React 内部输入类型和 lockfile 字段名误报。首屏文字符合虚构内容设计，无真实身份与履历。

有意差异：原创几何角色、抽象作品、通用叙事替代原个人内容；没有原照片、截图与业务演示。视觉核对依据是获准设计说明及本次通用设计，未读取原截图，也不声称逐像素复刻。

已知限制：未测试 Safari、Firefox 或真机触控。Three.js 桌面动态分块约 720 KB（gzip 187 KB），构建给出体积提示；窄屏与减少动态模式不请求该分块。没有进行发布时的完整依赖安全审计，公开前需重新检查。

扫描器自身包含通用拒绝模式示例，已单独人工复核；不随包保存原人物的姓名、拼音或地点黑名单。构建依赖中的通用数字与示例字符串不作身份断言，真实页面另经浏览器验证。
